---
- hosts: all
  become: true
  vars:
    habilitations_file_sops: "{{ lookup('first_found', playbook_dir ~ '/../../products/' ~ product ~ '/authorizations/habilitations.yml', errors='ignore') }}"
  roles:
    - setup
