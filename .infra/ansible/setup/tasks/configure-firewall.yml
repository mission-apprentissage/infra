- name: Suppression du paquet 'ufw'
  apt:
    name: ["ufw"]
    state: absent
    autoremove: true
    purge: true

- name: Copie du modèle de la configuration de nftables
  ansible.builtin.template:
    src: "{{ playbook_dir }}/../../products/{{ product }}/nftables.conf.jinja2"
    dest: "/etc/nftables.conf"
  notify:
    - restart-nftables

